export enum SidebarItemType {
  PROFILE = 'profile',
  SEARCH = 'search',
  NOTIFICATIONS = 'notifications',
  ARCHIVE = 'archive',
  SYNC = 'sync',
  HOME = 'home',
  SETTINGS = 'settings',
  CREATE = 'create',
}
