# 의존성 (builder 단계에서 pnpm install로 새로 설치)
node_modules
**/node_modules

# 빌드 산출물 / 캐시 (빌드 시 재생성)
.next
**/.next
.turbo
**/.turbo

# 시크릿 (이미지에 절대 포함 금지)
.env
.env.web
**/.env
**/.env.*

# VCS / 에디터 / 개발 도구
.git
.gitignore
.husky
.vscode
.claude

# 인프라 / 문서 (이미지 빌드에 불필요)
.github
nginx
compose.dev.yml
compose.prod.yml
docs
http
README.md

# 로컬 업로드 디렉토리
apps/api/uploads
